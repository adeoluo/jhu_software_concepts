# Read the Docs build config. In the Read the Docs project, set
# Admin > Settings > "Path for .readthedocs.yaml" to: module_4/.readthedocs.yaml
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"

sphinx:
  configuration: module_4/docs/source/conf.py

python:
  install:
    - requirements: module_4/requirements.txt
